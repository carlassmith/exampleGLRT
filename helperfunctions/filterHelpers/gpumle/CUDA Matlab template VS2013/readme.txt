Move zip to templates folder in documents

and run under Visual Studio 2010 / Visual Studio Tools/Developer Command Prompt).

Run the following command: devenv /installvstemplates.


%% A template is very simple to creat create a sample project and click export as template.

if error mex.h check win32/64 and for other check CUDA path in project and solution file.